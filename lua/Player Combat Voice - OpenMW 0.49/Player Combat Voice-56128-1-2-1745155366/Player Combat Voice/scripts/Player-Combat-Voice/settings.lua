-- settings.lua

local I = require('openmw.interfaces')

I.Settings.registerPage {
    key = 'PlayerCombatVoice', -- Keep the existing key
    l10n = 'PlayerCombatVoice', -- Keep the existing l10n
    name = 'Player Combat Voice', -- Keep the existing name
    description = 'By Xe', -- Keep the existing description
}