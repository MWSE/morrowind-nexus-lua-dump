local defaultConfig = {
	modEnabled = true,
    breakEnabled = true,
	basePick = 50,
    baseProbe = 50,
}

local mwseConfig = mwse.loadConfig("Krimson.Breakable Unlimited Thieves Tools", defaultConfig)

return mwseConfig