local this = {
  logic = require( "MAB0.logic" ),
  value = require( "MAB0.value" ),
  string = require( "MAB0.string" ),
  locale = require( "MAB0.locale" ),
  metadataController = require( "MAB0.metadataController" ),
  table = require( "MAB0.table" ),
  version = require( "MAB0.version" ),
  timer = require( "MAB0.timer" ),
  math = require( "MAB0.math" )
}

return this