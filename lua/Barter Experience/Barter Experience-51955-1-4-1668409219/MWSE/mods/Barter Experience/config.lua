--Mod by Muggins because nothing like this exists yet afaik--

local defaultConfig = {
    modEnabled = true,
    FlatRate = "10",
    ValueRate = "20",
}

local config = mwse.loadConfig("Barter Experience", defaultConfig)
return config